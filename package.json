{
  "name": "macy's-homepage-clone",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:all": "npm run build && npm --prefix server run build",
    "preview": "vite preview",
    "dev:server": "npm --prefix server run dev",
    "build:server": "npm --prefix server run build",
    "test:server": "npm --prefix server run test",
    "import:dresses": "npm --prefix server run catalog:import:dresses --",
    "import:ladies-shoes": "npm --prefix server run catalog:import:ladies-shoes --",
    "import:men": "npm --prefix server run catalog:import:men --",
    "check:images": "node scripts/validate-images.mjs"
  },
  "dependencies": {
    "lucide-react": "^0.563.0",
    "react": "^19.2.4",
    "react-dom": "^19.2.4"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "@vitejs/plugin-react": "^5.0.0",
    "autoprefixer": "^10.4.24",
    "postcss": "^8.5.6",
    "tailwindcss": "^3.4.17",
    "typescript": "~5.8.2",
    "vite": "^6.2.0"
  }
}
